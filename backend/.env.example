# Server
PORT=4000
NODE_ENV=development

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=12h
BCRYPT_ROUNDS=10

# Database (use ':memory:' for ephemeral, or a file path)
DB_PATH=./data/datalake.db
